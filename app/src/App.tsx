import { BrowserRouter, Routes } from "react-router-dom"

const App = () => {
  return <BrowserRouter>
    <Routes>
      <Route path="/" element={ } />
    </Routes>
  </BrowserRouter >
};

export default App;